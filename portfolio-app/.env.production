# API root for production builds when frontend is served separately (e.g., GitHub Pages)
# After deploying backend to Render, set this to your Render URL, e.g.:
# VITE_API_ROOT=https://financial-portfolio-xxxxx.onrender.com/t/a/s/d/z7p3q9x1
VITE_API_ROOT=

# Optional: base path if serving under a subfolder on GitHub Pages
# e.g., if publishing to https://asvanthc.github.io/portfolio/
# then set VITE_BASE=/portfolio/
VITE_BASE=
